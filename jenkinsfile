pipeline {
    agent any 
    stages {
	stage('Check config') {
            steps {
                echo 'mvn clean'
		cmd 'mvn clean' 
            }
        }
        stage('Compile') {
            steps {
                echo 'start building ...' 
		cmd 'mvn compile'
            }
        }
    }
}